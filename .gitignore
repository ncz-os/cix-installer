# Build outputs
build/iso-staging/
build/iso-staging-di/
build/r*-thin-stage/
build/r*-stage/
build/r*-efi-*.img
build/questing-bootstrap/
build/questing-mirror/
build/tool-cache/
build/refind-bin/
build/*.iso
build/*.qcow2
build/*.fd
build/*.sha256
build/*.bak-*
build/build-iso-di.sh.bak-*

# r73-bak (Codex review baseline — not committed)
post-install/.r73-bak/

# AI image-gen candidates (only canonical-promoted assets are committed)
assets/branding/_candidates/

# Upstream downloads
downloads/

# Gitignored asset blobs (supplied at build time from cixmini-gen-assets
# on ARGOS, or from a release artifact in CI)
assets/cix-debs/*.deb
assets/kernel/stable/
assets/kernel/edge/
assets/kernel/*.bin
assets/kernel/*.tgz
assets/sky1-firmware/
assets/npu/
!assets/npu/npu_embed_v2.py
assets/rootfs/

# NPU py3.11 uv venv toolchain: relocatable CPython 3.11 (python-build-
# standalone, install_only) + uv static binary. Supplied at build time;
# 46-python311.sh consumes these offline-first. See docs/POST-CIX-NPU-EMBEDDINGS-DRAFT.md.
assets/python311/

# Autoinstall scaffolding (work-in-progress, not r74-shippable)
autoinstall/

# Editor cruft
.DS_Store
*.swp
*~

# Python / shell editor leftovers
__pycache__/
*.pyc
build/apt-repo/*.deb
build/apt-repo/Packages.gz
public/
# Generated mirror/bootstrap/build scratch artifacts
build/resolute-bootstrap/
build/resolute-mirror/
build/apt-repo/
build/*.log
build/*.orig
build/*.patch
build/*.rej
build/.iso-*-done
preseed/*.orig
preseed/*.patch
preseed/*.rej
build/keys/

# Large rebuildable build artifacts (do NOT commit - regenerated by build scripts)
build/cix-repo.squashfs
build/ota-repo/
build/server-mirror/
build/kernel-debs/
*.r97bak
build/*.squashfs

# Mesa 26.1.3 GPU compute stack (built from source on .66; staged at build time)
assets/gpu/*.tar.gz
assets/gpu/*.tar.zst

# r130 fat-ISO + rescue build artifacts (rebuildable; do NOT commit)
build/desktop-mirror/
build/desktop-mirror.gaps
build/rescue-rootfs/
build/r*-gitlab-upload.json
assets/rescue/*.tar.zst
preseed/*.pre-btrfs.*
assets/mgmt/*.tar.zst
build/mgmt-rootfs/
post-install/buildkite-read-token
