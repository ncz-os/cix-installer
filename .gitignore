
*~
# 100MB per-file limit. Staged onto the build host like assets/cix-debs and
# 26.7: retired kernel channels (build artifacts like stable/edge; pruned from base.squashfs)
# 46-python311.sh consumes these offline-first. See docs/POST-CIX-NPU-EMBEDDINGS-DRAFT.md.
# .66:/home/jasonperlow/sinty-build/nm-build/out/sinty-nmd)
# AI image-gen candidates (only canonical-promoted assets are committed)
assets/agent-images/*.oci.tar
assets/agent-images/*.tar
assets/branding/_candidates/
assets/cix-debs/*.deb
assets/gpu/*.tar.gz
assets/gpu/*.tar.zst
assets/kernel/*.bin
assets/kernel/edge/
assets/kernel/legacy/
assets/kernel/retired-*/
assets/kernel/*.tgz
# 26.7: kernel DKMS source trees (mali/panthor/vpu/npu) are committed, but
# the in-tree build artifacts produced during .ko generation (intermediate
# .o, .cmd, .mod, Module.symvers, Makefile-local) are not. Rebuild from
# any commit with `bash build/build-kernel-debs.sh`.
# Keep this pattern narrow so it doesn't accidentally match the .ko files
# in the kver-overlay directories (e.g. mali/7.2.0-sky1-ncz/mali_kbase.ko
# which IS what we ship).
assets/kernel/*/src/**/*.o
assets/kernel/*/src/**/*.d
assets/kernel/*/src/**/*.cmd
assets/kernel/*/src/**/*.mod
assets/kernel/*/src/**/*.mod.c
assets/kernel/*/src/**/Module.symvers
assets/kernel/*/src/**/modules.order
assets/kernel/*/src/**/*.ko
assets/kernel/*/src/**/GPU_OBJ
# panthor source is flat (no src/ subdir) — exclude its build artifacts
assets/kernel/panthor/*.o
assets/kernel/panthor/*.d
assets/kernel/panthor/*.cmd
assets/kernel/panthor/*.mod
assets/kernel/panthor/*.mod.c
assets/kernel/panthor/Module.symvers
assets/kernel/panthor/modules.order
assets/kernel/panthor/panthor.ko
assets/kernel/panthor/panthor.mod
assets/mgmt/*.tar.zst
assets/models/nomic-embed-text-*.cix
assets/npu/*
!assets/npu/ncz-npu-embed-server.py
!assets/npu/npu_embed_v2.py
!assets/npu/ssdt-npucre.asl
assets/python311/
assets/refind/*.bak-pre-*
assets/rescue/*.tar.zst
assets/rootfs/
assets/singularity-boot-splash/singularity-boot-splash
assets/singularity/*.tgz
assets/sinty-nm/sinty-nmd
assets/sky1-firmware/
assets/squashfs/
assets/vivaldi/*.deb
assets/vivaldi/*.gpg
assets/zoder/
autoinstall/
# Autoinstall scaffolding (work-in-progress, not r74-shippable)
# baked into the ISO from there; rebuild instructions in assets/models/MODELS-README.md.
# below (npu_embed_v2.py was ignored and untracked despite the ! rule).
build/apt-repo/
build/apt-repo/*.deb
build/apt-repo/Packages.gz
build/*.bak-*
build/build-iso-di.sh.bak-*
# (build/build-singularity.sh also folds it into the /opt/singularity payload;
build/cix-repo.squashfs
build/desktop-mirror/
build/desktop-mirror.gaps
build/*.fd
build/*.iso
build/.iso-*-done
build/*.iso.*-part*
build/iso-staging/
build/iso-staging-di/
build/kernel-debs/
build/libdrm-debs/
build/keys/
build/*.log
build/mgmt-rootfs/
build/*.orig
build/ota-repo/
# Build outputs
build/*.patch
build/*.qcow2
build/questing-bootstrap/
build/questing-mirror/
build/r*-efi-*.img
build/refind-bin/
build/*.rej
build/rescue-rootfs/
build/resolute-bootstrap/
build/resolute-mirror/
build/r*-gitlab-upload.json
build/r*-stage/
build/r*-thin-stage/
build/server-mirror/
build/*.sha256
build/*.squashfs
build/tool-cache/
build/usb-recovery-deb/
# DIRECTORY, so the old `assets/npu/` form silently defeated the negations
downloads/
.DS_Store
# Editor cruft
# Generated mirror/bootstrap/build scratch artifacts
# git and let build/build-npu-ssdt.sh regenerate npu-acpi-override.cpio.
# Gitignored asset blobs (supplied at build time from cixmini-gen-assets
# Large NOE graphs: the nomic-embed-text-v1.5 .cix is ~500MB, past GitLab's
# Large rebuildable build artifacts (do NOT commit - regenerated by build scripts)
# Mesa 26.1.3 GPU compute stack (built from source on .66; staged at build time)
# MNEMOS offline container image — build-time OCI archive (~260MB, staged via
# `ncz install mnemos`. Regenerate with assets/agent-images/README.md.
# NFS/build-host like base.squashfs). Bundled into the ISO for offline
# NOTE the trailing /* -- git cannot re-include a file inside an EXCLUDED
# NPU py3.11 uv venv toolchain: relocatable CPython 3.11 (python-build-
# of this table was an unexplained binary blob nobody could rebuild.
# on ARGOS, or from a release artifact in CI)
post-install/buildkite-read-token
post-install/.r73-bak/
preseed/*.orig
preseed/*.patch
preseed/*.pre-btrfs.*
preseed/*.rej
public/
*.pyc
__pycache__/
# Python / shell editor leftovers
# r130 fat-ISO + rescue build artifacts (rebuildable; do NOT commit)
# r181: Vivaldi browser .deb — build-time blob (staged via NFS, like base.squashfs)
# r73-bak (Codex review baseline — not committed)
*.r97bak
# Recovered 2026-08-02 from an installed board -- until then the only copy
# rEFInd banner pre-robot backup (dev artifact)
# singularity-boot-splash — native KMS boot splash, build-time blob
# Singularity Desktop payload — build-time blob (staged via NFS/ARGONAS, like base.squashfs)
# sinty-nm daemon — build-time blob (build singularityos-lab/sinty-nm; fetch from
# split-release ISO parts (leftover build artifacts)
# squashfs layer artifacts are BUILD OUTPUTS (1-2GB) — NFS/releases only, never git
# standalone, install_only) + uv static binary. Supplied at build time;
*.swp
# The NPU ACPI SSDT override is REPRODUCIBLE FROM SOURCE: keep the ASL in
# this staged copy is a fallback for 60-boot-splash). arm64 ELF from .66.
# Upstream downloads
build/forky-vendor-mirror/
build/forky-mirror/
build/gst-cix-out/

# User-space tool extraction cache (debootstrap, xorriso, 7z .debs extracted to
# build/tools/extracted/ — gitignored so the build host doesn't accidentally
# commit a copy of /usr/sbin/debootstrap into the repo)
build/tools/
